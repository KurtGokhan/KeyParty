---
"keyparty": patch
---

Give the macOS **.dmg** a branded install window instead of the bare default Finder folder: a light party-gradient backdrop with a "drag the keycap into your Applications folder" arrow, the app and Applications icons positioned over it, and the app's own icon as the disk's volume icon.
