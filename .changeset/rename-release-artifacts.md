---
"keyparty": patch
---

Name the downloadable builds **KeyParty.exe** and **KeyParty.dmg** (was `keyparty-windows.exe` / `keyparty-macos.dmg`). The in-app download links and the stable `releases/latest/download/…` URLs are updated to match.
